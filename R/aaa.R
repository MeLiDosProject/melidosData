present <-  issue <- expected <- actual <- .var <- .type <-
  `Choices, Calculations, OR Slider Labels` <- `Variable / Field Name` <-
  `Text Validation Type OR Show Slider Number` <- `Field Label` <-
  `Form Name` <- `Field Type` <-
  NULL

utils::globalVariables("codebook_strings_to_ignore")
