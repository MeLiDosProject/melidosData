% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_labels.R
\name{extract_labels}
\alias{extract_labels}
\title{Extract labels from a list of objects}
\usage{
extract_labels(data)
}
\arguments{
\item{data}{A list of objects. Each element is expected to have a \code{"label"}
attribute (e.g., created via \code{attr(x, "label") <- "some label"}).}
}
\value{
A character vector containing the \code{"label"} attribute of each
element in \code{data}. If an element does not have a \code{"label"} attribute,
\code{NA_character_} is returned for that element.
}
\description{
Retrieves the \code{"label"} attribute from each element in a list and returns
a character vector of labels.
}
\examples{
# Create example data with labels
x1 <- 1:3
attr(x1, "label") <- "First variable"

x2 <- 4:6
attr(x2, "label") <- "Second variable"

x3 <- 7:9
# No label set for x3

data <- list(x1, x2, x3)

# Extract labels
extract_labels(data)

# Works for data frames as well
data <- data.frame (x1, x2, x3)
extract_labels(data)
}
