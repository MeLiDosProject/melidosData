% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/REDCap_factors.R
\name{REDCap_factors}
\alias{REDCap_factors}
\title{Convert REDCap choice fields to factors}
\usage{
REDCap_factors(
  data,
  lookup,
  var_col = `Variable / Field Name`,
  type_col = `Field Type`,
  levels_col = `Choices, Calculations, OR Slider Labels`,
  radio_value = c("checkbox", "radio"),
  warn = TRUE
)
}
\arguments{
\item{data}{Data frame containing REDCap records.}

\item{lookup}{Data frame containing variable metadata.}

\item{var_col}{Unquoted column in \code{lookup} with variable names.}

\item{type_col}{Unquoted column in \code{lookup} with REDCap field types.}

\item{levels_col}{Unquoted column in \code{lookup} with coded levels
(\code{"1, Yes | 0, No"} format).}

\item{radio_value}{Character vector of field types to convert.}

\item{warn}{Logical; warn when variables are in \code{lookup} but absent from
\code{data}.}
}
\value{
\code{data} with selected columns converted to factors.
}
\description{
Applies factor levels and labels using REDCap dictionary definitions for
\code{radio} and \code{checkbox} fields (configurable via \code{radio_value}).
}
\examples{
dict_path <- system.file("ext", "DataDictionary_chronotype.csv",
  package = "melidosData"
)
dict <- utils::read.csv(dict_path, check.names = FALSE)
dict <- REDCap_codebook_prepare(dict, form.filter = "mctq")

chronotype_with_factors <-
REDCap_factors(
  data = REDCap_example_chronotype,
  lookup = dict
)

chronotype_with_factors$mctq_work_travel
#original:
REDCap_example_chronotype$mctq_work_travel
}
